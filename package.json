{
  "name": "beanify-autoload",
  "version": "1.0.2",
  "description": "beanify-autoload ",
  "main": "index.js",
  "scripts": {
    "test": "standard && tap test/*.test.js --100 --cov --coverage-report=text",
    "unit": "tap test/*.test.js --100 --cov --coverage-report=lcov"
  },
  "keywords": [
    "beanify",
    "beanify-plugin",
    "beanify-autoload",
    "micro-service"
  ],
  "author": "beanjs",
  "license": "MIT",
  "dependencies": {
    "beanify": "^1.0.9",
    "beanify-plugin": "^1.0.6",
    "steed": "^1.1.3"
  },
  "devDependencies": {
    "standard": "^14.3.1",
    "tap": "^14.9.2"
  },
  "repository": {
    "url": "git+https://github.com/beanjs-framework/beanify-autoload.git",
    "type": "git"
  }
}
